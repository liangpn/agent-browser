# Block Chrome remote debugging HTTP/WebSocket endpoints.
# The Minimal image only allows MCP, VNC, and /tickets externally.

location /json {
    return 403;
}

location /devtools/ {
    return 403;
}

